// Debug — apunta al backend del Mac en la misma red local
// Note: xcconfig treats // as a comment, so URLs use the SLASH variable trick
SLASH = /
// Si cambia la IP del Mac en el router, actualiza esta línea.
BASE_URL = http:$(SLASH)$(SLASH)192.168.1.82:8080
